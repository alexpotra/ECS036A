#include <stdio.h>
#include <stdlib.h>
#include <math.h>

// create a point struct with coordinate values (x,y)
typedef struct Point
{
    int x;
    int y;
} Point;

make_points(int n, FILE *filename)
{
}

int main(int argc, char *argv[])
{
    printf("HI");
    return 0;
}
